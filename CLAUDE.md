# tsukuras-mcp-server

北海道建設業データベース（Tsukuras）をMCP経由でAIエージェントに提供するCloudflare Workersサーバー。Smithery/Gumroad/Zennで展開する「Japan Data」MCPシリーズの1つ。

## セッション終わりの記録ルール（2026-07-29策定）

Claude Codeの使用量節約のため、セッションを長く続けず、区切りごとに記録を残して次のセッションへ引き継ぐ。

- 続きがあるタスクなら、セッションの終わりに「今どこまで進んだか・次に何をやるか」を短く書き残す（このファイルの下に一時的に追記、またはPR/issueコメントで可）。次のセッションはそれを読めばよく、会話ログを掘り起こす必要をなくす。
- 仕組み（ワークフロー・スキーマ・デプロイ設定等、他へ影響しうる変更）に変更を加えた場合のみ、変更履歴を残す。このリポジトリに現状changelog/引き継ぎ書は存在しないため、必要になった時点で作成すればよい。記事内容・データ更新など日常運用の変更は記録不要。
